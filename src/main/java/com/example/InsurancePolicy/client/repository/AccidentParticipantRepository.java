package com.example.InsurancePolicy.client.repository;

import com.example.InsurancePolicy.client.entity.AccidentParticipant;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccidentParticipantRepository extends JpaRepository<AccidentParticipant, Long> {}